// Sahne ProMax — modular server entry point.
// Delegates to server/index.js while retaining 100% backward compatibility for existing callers and test suites.
'use strict';

module.exports = require('./index.js');
